import { Layout } from 'antd'
const { Header, Footer, Sider, Content } = Layout

function FlightList() {
  return <div className="booking-page">Booking page content</div>
}

export default FlightList
