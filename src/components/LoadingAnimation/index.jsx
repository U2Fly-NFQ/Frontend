const loadingStyle = {
  maxWidth: '100%',
  objectFit: 'contain',
}

const LoadingAnimation = () => {
  return <img style={loadingStyle} alt="Loading animation" />
}

export default LoadingAnimation
